---
name: init
description: Initialize project knowledge structure — creates project.yaml and decisions/
---

Use the `init` subagent to handle this request.

The init subagent creates `.claude/project.yaml` and `.claude/decisions/` directory structure for a new project.

Typical usage:
- `/init` — at project start, set up knowledge structure
